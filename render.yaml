services:
  - type: web
    name: agrimap-django
    env: docker
    dockerfilePath: ./Dockerfile.django
    dockerContext: .
    healthCheckPath: /api/health/
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: server_agri_map_django.settings.production
      - key: DATABASE_URL
        fromDatabase:
          name: agrimap-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: YOUR_RENDER_APP_NAME.onrender.com,localhost,127.0.0.1
      - key: CORS_ALLOWED_ORIGINS
        value: https://YOUR_RENDER_APP_NAME.onrender.com
      - key: DEBUG
        value: "False"
      - key: GUNICORN_WORKERS
        value: "4"

databases:
  - name: agrimap-db
    databaseName: agrimap
    user: agrimap
    plan: starter
    postgresMajorVersion: 16
